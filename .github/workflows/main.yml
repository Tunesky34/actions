name: Node CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
        run: npm install
      - name: Run tests
        run: npm test
